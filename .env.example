# Virlo API — https://virlo.ai/developers
VIRLO_API_KEY=

# Gemini API (for Imagen image generation)
GEMINI_API_KEY=

# Blotato API (for TikTok drafts + visual creation)
BLOTATO_API_KEY=

# ScrapeCreators API (per-post TikTok metrics)
SCRAPECREATORS_API_KEY=
FIRECRAWL_API_KEY=
# Server-only. Required by the private Creddy ingestion worker; never expose it
# in browser code or copy it into the Creddy app repository.
SUPABASE_SERVICE_ROLE_KEY=
CREDDY_SUPABASE_URL=
CREDDY_SUPABASE_ANON_KEY=
AI_PROVIDER=openai
# Creddy uses Codex Scheduled Tasks by default. OPENAI_API_KEY is needed only
# if CREDDY_AI_EXECUTION_MODE is intentionally changed to openai_api.
CREDDY_AI_EXECUTION_MODE=codex_scheduled
OPENAI_API_KEY=
CREDDY_PIPELINE_ENABLED=false
CREDDY_CAMPAIGN_SLUG=credit-card-rewards
CREDDY_DATA_ROOT="/Users/your-user/Documents/ChatGPT/Social media automation data"
CREDDY_MAX_LINKS_PER_SOURCE=20
CREDDY_MAX_ARTICLE_SCRAPES=40
CREDDY_RECHECK_HOURS=24
VIDEO_FACTORY_BASE_URL=http://127.0.0.1:4300
CREDDY_BACKGROUND_MUSIC_PATH=
CREDDY_PUBLISH_LEAD_MINUTES=15
# Absolute protected JSON registry for approved referral IDs and destinations.
CREDDY_REFERRAL_REGISTRY_PATH=
CREDDY_WEBSITE_REPOSITORY_PATH="/Users/your-user/Code/credit-card-rewards-creddy"
# Explicit external-write gate for agent-8-website-pr. Keep false for local sync checks.
CREDDY_WEBSITE_PR_ENABLED=false
# Scalable blog publishing. Enable only after Agent 7 approval and the website
# CMS migration have been verified. New articles do not require Git or Vercel.
CREDDY_WEBSITE_CMS_PUBLISH_ENABLED=false
CREDDY_WEBSITE_AUTO_PUBLISH=false
CREDDY_WEBSITE_AUTO_PUBLISH_ON_APPROVAL=false
# Optional Agent 8 storage optimization. Requires `cwebp` (`brew install webp`).
CREDDY_WEBSITE_ASSET_WEBP_ENABLED=false
CREDDY_WEBSITE_ASSET_WEBP_QUALITY=88
# One-run maintenance switch for replacing existing approved CMS assets.
CREDDY_WEBSITE_CMS_FORCE_REPUBLISH=false
CREDDY_WEBSITE_BASE_URL=https://getcreddy.com
CREDDY_WEBSITE_REVALIDATE_SECRET=
# Live getcreddy.com publishing credentials remain intentionally unset until
# the single-pipeline article flow passes supervised review.
CREDDY_WEBSITE_PUBLISH_URL=
CREDDY_WEBSITE_PUBLISH_TOKEN=
SLACK_BOT_TOKEN=
# App-level xapp token with connections:write. Enables Slack Socket Mode so
# review buttons work without Cloudflare, ngrok, Tailscale, or any public URL.
SLACK_APP_TOKEN=
SLACK_SIGNING_SECRET=
SLACK_APPROVAL_CHANNEL_ID=
# Creddy lifecycle updates. Agent 7 rich review messages require a bot with
# chat:write + files:write; the bot must be invited to this channel.
SLACK_SOCIAL_UPDATES_WEBHOOK_URL=
SLACK_SOCIAL_UPDATES_CHANNEL_ID=
# Optional. Only needed when browser links to the local portal must be public.
# Agent 7 review, approval, and rejection work entirely in Slack without it.
DASHBOARD_BASE_URL=
